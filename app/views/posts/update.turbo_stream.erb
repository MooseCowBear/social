<%= turbo_stream.replace @post do %>
  <%= render partial: "posts/post", locals: { post: @post, show: false, curr: current_user } %>
  <%= turbo_stream.update "flash", partial: "layouts/flash" %>
<% end %>
<div class="page__wrapper">
  <h1>Find New Friends</h1>
  <%= render "search" %>
  <div class="user_index__wrapper">
    <%= render @users, locals: { request: true } %>
    <%= "We couldn't find anyone else for you to friend." unless @users.any? %>
  </div>
</div>